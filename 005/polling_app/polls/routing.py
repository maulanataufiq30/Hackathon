from django.urls import re_path
from . import consumers

websocket_urlpatterns = [
    # Untuk saat ini kosong, karena kita menggunakan SSE bukan WebSocket
    # Bisa ditambahkan nanti jika diperlukan WebSocket
]

